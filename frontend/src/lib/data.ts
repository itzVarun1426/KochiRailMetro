import type { Kpi, JobCard, Train, DepotLayout, BrandingDetails, CleaningDetails, CertificateDetails } from './types';

// Note: KPI data is now fetched dynamically via dashboardService
// Note: Job card data is now fetched dynamically via jobCardService

// Note: Train data is now fetched dynamically via trainService
// Note: Depot layout is now calculated dynamically based on current train positions
